<form method="post" action="{{ path('mutuel_delete', {'id': mutuel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mutuel.id) }}">
    <button class="btn btn-danger"><i class="fa fa-save"></i>  Supprimer</button>
</form>
